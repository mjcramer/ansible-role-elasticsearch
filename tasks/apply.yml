# roles/mesos-master/tasks/apply.yml
---
#- name: apply | create folders
#  file:
#    path: "{{ item }}"
#    state: directory
#    owner: "{{ mesos_user }}"
#    group: "{{ mesos_group }}"
#    mode: 0775
#  with_items:
#    - "{{ mesos_master.conf_dir }}"
